// NextAuth configuration stub
// Replace providers and secret in your environment before enabling

export const authOptions = {
  // providers: [ ... ],
  // callbacks: { ... }
};

export default function getAuth() {
  throw new Error('Configure NextAuth in lib/auth.js before using auth routes.');
}
